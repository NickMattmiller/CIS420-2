@model asp.netmvc5.Models.GrantManagerModel

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@{
    if (Session["GrantMessage"] != null)
    {
        <div class="alert alert-success alert-dismissable">@Session["GrantMessage"]</div>
        Session["GrantMessage"] = null;
    }
}

<h2></h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>GrantManagerModel</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Grant_Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Grant_Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Grant_Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Grant_Description)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Type)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Type)
        </dd>

    </dl>

    @Html.Action("GrantFilesPartial", "GrantManagerModels", new { GrantDescription = Model.Grant_Description })

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
